# --- Gateway (serves UI + proxies /api/*) ---
PORT=3000
AUTH_URL=http://localhost:3001

# --- Auth service (API + SQLite) ---
# PORT=3001
# Required in production — used to sign short-lived access JWTs
ACCESS_TOKEN_SECRET=use-a-long-random-string
